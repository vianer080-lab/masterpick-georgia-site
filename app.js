async function load(){const box=document.querySelector('#products');try{const r=await fetch('./products.json',{cache:'no-store'});const items=await r.json();box.innerHTML=items.map((x,i)=>`<article class="card"><div class="productVisual"><span>${icon(x.title)}</span><small>MASTERPICK</small></div><div class="cardBody"><div class="cardTop"><span class="store">${x.store}</span><span class="verified">✓ ПОДБОРКА</span></div><h3>${x.title}</h3><p>${x.description}</p>${x.url&&x.url!=='#'?`<a class="buy" href="${x.url}" target="_blank" rel="nofollow sponsored noopener">Смотреть на Amazon →</a>`:'<span class="soon">Скоро</span>'}</div></article>`).join('')}catch(e){box.innerHTML='<article class="card"><h3>Подборки обновляются</h3><p>Попробуйте открыть страницу немного позже.</p></article>'}}function icon(t){t=t.toLowerCase();if(t.includes('автомоб')||t.includes('obd')||t.includes('компресс'))return'🚗';if(t.includes('заряд')||t.includes('кабел')||t.includes('аккумуля'))return'⚡';if(t.includes('науш'))return'🎧';if(t.includes('сетев'))return'🔌';return'★'}load();