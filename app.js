async function load(){const box=document.querySelector('#products');try{const r=await fetch('./products.json',{cache:'no-store'});const items=await r.json();box.innerHTML=items.map(x=>`<article class="card"><span class="store">${x.store}</span><h3>${x.title}</h3><p>${x.description}</p>${x.url&&x.url!=='#'?`<a href="${x.url}" target="_blank" rel="nofollow sponsored noopener">Посмотреть →</a>`:'<span style="color:#60728b">Скоро</span>'}</article>`).join('')}catch(e){box.innerHTML='<article class="card"><h3>Подборки обновляются</h3><p>Попробуйте открыть страницу немного позже.</p></article>'}}load();